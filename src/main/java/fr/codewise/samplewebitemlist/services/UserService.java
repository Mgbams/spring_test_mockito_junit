package fr.codewise.samplewebitemlist.services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import fr.codewise.samplewebitemlist.repositories.UserRepository;

/*@Service
public class UserService {
	@Autowired
	UserRepository userRepository;
	
	public String getNameByUserId(Long id) {
		String name = userRepository.findNameById(id);
		return name;
	}
	
	public String getEmailByUserId(Long id) {
		String email = userRepository.findEmailById(id);
		return email;
	}
	
	
}*/
